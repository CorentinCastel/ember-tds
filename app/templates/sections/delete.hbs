{{page-title "Delete"}}
<br>
<div class="ui container">
  <div>Êtes-vous sûr de vouloir supprimer cette section et ses {{@model.products.length}} éléments ?</div>
  <button class="ui button red" {{action "confirm" @model.section}}> Confirmer </button>
  <button class="ui button secondary" {{action "annuler"}}>Annuler</button>
</div>
{{outlet}}