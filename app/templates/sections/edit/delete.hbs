{{page-title "Delete"}}
<br>
<div class="ui container">
  <div>Êtes-vous sûr de vouloir supprimer ce produit ({{@model.product.name}}) ?</div>
  <button class="ui button red" {{action "confirm" @model}}> Confirmer </button>
  <button class="ui button secondary" {{action "annuler" @model}}>Annuler</button>
</div>
{{outlet}}