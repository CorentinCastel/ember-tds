{{page-title "Add"}}
<div class="row">
  <div class="col">
    <Input @value={{@model.nom}} placeholder="Entrez le nom de la catégorie" class="form-control"/>
  </div><
  <button class="btn btn-primary" {{action "save" @model}}> Enregister </button>
  <button class="btn btn-secondary" {{action "annuler"}}>Annuler</button>
</div>
{{outlet}}